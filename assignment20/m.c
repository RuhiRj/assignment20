#include<stdio.h>
#include<conio.h>
/*#include<stdlib.h>
int main()
{
    int *ptr;
    int n,i;
    printf("enter the number of the elements:");
    scanf("%d",&n);
    ptr=(int*)calloc(n,sizeof(int));
    if(ptr==NULL)
        printf("malloc is not allocated the memory:\n ");
    else
        printf("memory is allocated :\n");
    printf("enter the element from the user%d:\n",n);
    for(i=0; i<n; i++)
        scanf("%d",&ptr[i]);
    for(i=0; i<n; i++)
        printf("%d\n",ptr[i]);
    return 0;
}*/

